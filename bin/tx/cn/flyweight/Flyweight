享元模式：轻量级模式 对象结构模式

通过共享技术实现相同或相似对象的重用
逻辑上是多个对象，物理上他们共享一个享元对象！！！！！！
存储这个实力的对象称为享元池~！！！享元工厂类
如下角色：1.抽象享元模式Flyweight：定义具体的享元类的公共方法--这些方法可以向外部提供享元对象的内部数据（内部状态）
同样也可以通过这些方法设值外部数据（外部状态） 2.具体享元类 实现接口-享元对象， 在具体享元类中为内部状态提供存储空间，由于
具体享元对象必须是可以共享的，因此所存储的对象必须是内部的--可以结合单例模式设计具体享元类 3.非共享具体享元类 当需要一个非具体享元类乐意直接实例化创建
此类还可以将具体享元对象作为子节点
4.享元工厂类 创键并管理享元对象， 又一个额享元池把具体享元类放进去 

内部状态是存储在享元对象内部并不会随环境的改变状态，因此可以共享
外部状态是随环境改变而变化 不共享有客户端保存